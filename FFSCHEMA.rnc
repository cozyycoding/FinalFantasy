datatypes xsd = "http://www.w3.org/2001/XMLSchema-datatypes"

start = scene
scene = element scene { box+, name }
name = attribute name { text }
box = element box { choice }
choice = element choice { speaker }
speaker = element speaker { text }